# 115-Station 使用说明

面向日常使用的操作手册。部署方式见 [README.md](README.md)。

登录管理后台后，左侧菜单依次为：**仪表盘 / 账号管理 / Strm 管理 / 自动整理 / 上传下载 / 系统配置 / 消息配置 / 扩展功能 / 实时日志**。地址栏与页面一一对应（`/sync`、`/organize`、`/plugins`……），可直接收藏或刷新。

所有带 **?** 问号图标的字段都有气泡说明，鼠标悬停（手机上点按）即可查看字段含义。

---

## 1. 管理员账号与登录

管理员账号由**容器环境变量**提供（网页注册已移除），在 `docker-compose.yml` 中配置：

```yaml
environment:
  - AUTH_USER=admin          # 管理员用户名
  - AUTH_PASSWORD=你的强密码  # 修改后 docker compose up -d 重启生效
```

1. 浏览器打开 `http://IP:6060`，用上面的账号密码登录
2. **改密码 = 改环境变量后重启容器**（启动时自动同步，环境变量为权威来源）
3. 忘记密码：改环境变量重启即可；或 `docker exec 115-station ./strmhub --reset-admin` 删除凭据文件后重启重建
4. 从未配置环境变量且无历史账号时，首次启动会自动生成随机密码（用户名 `admin`），在容器启动日志中查看：`docker logs 115-station`

## 2. 账号管理（登录 115）

两种登录方式，任选其一，可同时配置互为备份：

| 方式 | 说明 |
|---|---|
| **扫码登录** | 点击扫码，用 115 App 扫二维码。适合个人 Cookie 方式，兼容性最好 |
| **OpenAPI 授权** | 115 开放平台 PKCE OAuth 授权，Token 自动刷新，长期运行更稳定 |

登录后可在此调整 **API 请求间隔**（读接口最小间隔，默认 1 秒）。如遇到「服务器开小差了」等风控提示，把间隔调大（如 1500-2000ms）。

> 写入类操作（重命名 / 移动 / 删除 / 转存 / 上传）固定 ≥3 秒间隔，这是风控底线，不建议调低。

## 3. Strm 管理（115 → 本地 STRM）

页面地址 `/sync`，三个页签：

| 页签 | 内容 |
|---|---|
| **STRM 配置** | 直链域名、直链格式、是否保留后缀、本地已存在时覆盖还是跳过。全量与增量生成 STRM 都按这里的规则写 |
| **全量同步** | 网盘媒体库 cid、本地媒体目录、三组文件后缀、同步模式，以及失效 STRM 检测 |
| **增量同步** | 增量轮询间隔与事件流状态（自动整理的 Cron 在「自动整理 → 基础配置」） |

### 全量同步

遍历网盘目录树，视频生成 STRM，附属文件（字幕/NFO/图片）按后缀配置下载。首次使用和大范围变动后跑。

- **网盘媒体库 cid**：115 里媒体库所在的目录，增量同步与整理入库都锚定它
- **本地媒体目录**：STRM 生成位置（容器内默认 `/media`，需与 Emby 挂载一致）
- **同步模式**：标准模式逐目录遍历，兼容性最好；快速模式一次取回整棵目录树，请求数少两个数量级，
  但依赖 115 客户端端点，接口变动时自动降级为标准模式

### 失效 STRM 检测

「失效 STRM」= 本地还有 .strm / 附属文件，但网盘上的源文件已经没了。生活事件有窗口，
网页版批量删除、停机期间的删除增量同步都收不到，只有整库扫描做一次差集才查得出来。

打开开关后，每次全量同步结束会标出失效条目并在页面列出预览；**只标记不删**，
点「清理全部」确认后才真删本地文件与台账（网盘不受影响）。清单不完整时后端会跳过标记，
避免把没取全的文件误判成已删除。

开关打开后还会多出 **定时全量同步**：按 Cron 定期跑一次全量刷新失效标记，建议每天最多一次、
放在夜间（整库扫描请求量大）。定时同样只标记不删除。检测关掉时这个开关不显示，后台也不会执行。

### 增量同步

基于 115 生活事件只处理新增 / 变更，不遍历全盘。**前提**：115 生活 APP 里开启「最近」，
且已经跑过一次全量同步。

增量是**独立轮询**，默认 30 秒一轮（最小 15 秒），只管网盘端的外部变更：手机/客户端上传、
离线下载完成落库、网页端的删除与改名。一轮通常只发 1~2 个请求，空转时完全静默。
工作区（待整理 / 转存）里的变动不归它管；整理自己搬动改名的文件登记在案，事件绕回来时直接跳过。

间隔填 **0** = 关闭独立轮询，增量就没有自己的时间表了：只在「自动整理」的 cron 命中时
跟着串行跑一次；那条 cron 也留空的话，增量完全不会自动执行。

页面下方的**事件流状态**卡片回答「为什么没同步」：生活事件开关、当前通道、上一轮结果、
积压事件数；「测试事件流」按钮只读不处理（不推进游标、不落库、不动本地文件）。

## 4. 自动整理（整理 115 网盘内的文件）

把网盘里命名混乱的文件识别、分类、改名、归位。配置分这几页：

### 基础配置
- 整理开关与工作目录（网盘内待整理 → 整理完成后的去向）
- 识别不到时的处理（保留 / 移动到指定目录）
- **自动整理 Cron**：命中时跑一遍整理流水线（扫待整理目录 → 识别 → 分类 → 洗版 → 重命名 →
  搬入媒体库 → 写 STRM → 刮削 → 刷 Emby），默认 `*/10 8-23 * * *`。它只管「定时」这一种触发：
  转存 / 离线下载完成（勾了自动整理）、转存目录守望者、企微「整理」指令都会立即触发整理，
  与 cron 无关；留空只是不再定时跑

### 识别规则
文件名送去 TMDB 搜索**之前**的预处理与过滤。只影响识别，不改网盘里的文件名。

- **替换规则**：一条一行地加，每条自己选「文本」（一模一样才替换）或「正则」，替换内容留空 =
  删掉。不知道写什么就点 **「常用规则」** 勾现成的（去【】宣传块、去站点水印前缀、去字幕语种标注、
  下划线换点…）；下面的**效果预览**能粘一个真实文件名当场试算，看命中哪几条、结果长什么样。
  规则从上往下依次套用，后一条作用在前一条的结果上
- **发布组**：默认只认「文件名末尾 `-GROUP`」，`[WiKi] Show.S01E01.mkv` 这种认不出来；
  填进名单的组名在文件名任何位置出现都算命中（完整片段才算，填 `CR` 不会误伤 `CRUNCHYROLL`）。
  用于重命名模板的 `{resource_team}` 和洗版的 `resource_team` 匹配
- **最小视频大小**：小于该 MB 数的视频不识别、不入库，直接移到冗余——挡的是预告片、引流视频、
  封面动图。0 = 不限制，常见 50~200

> 后端正则是 Go 的 RE2：不支持先行/后行断言与反向引用，写了这些的规则整理时会被整条跳过（界面会当场提示）。

### AI 增强识别
前面所有 TMDB 搜索策略都失败时才调用：把原始文件名交给大模型提取「规范片名 + 年份」，
再回 TMDB 搜一轮（片名/年份以模型给的为准）。日常几乎不消耗额度。

按 OpenAI 协议标准调用：**地址填各家文档里给的 base_url，后面追加 `/chat/completions`**——
`/v1` 属于地址本身，不会替你猜。DeepSeek 官方是 `https://api.deepseek.com`（→ `/chat/completions`），
OpenAI 是 `https://api.openai.com/v1`（→ `/v1/chat/completions`），硅基流动、Ollama、vLLM 同理；
填整条完整路径也认。少写 `/v1` 导致 404 时会自动换带 `/v1` 的那条再试一次，
「测试连接」回显的就是真正打通的那条地址，照着把地址补全即可。

顶上有**启用开关**，关着时整理流程完全不碰模型接口，下面填好的地址/密钥/模型原样留着，随时能开回来。

**开启状态下保存，会先校验三项必填（地址 / 密钥 / 模型名）再跑一次连通性测试，通不过就不落库**——
这张卡只在「TMDB 全都搜不到」时才被用到，存错了当场没反馈，要等某次整理识别失败翻日志才发现。
关着时不校验，直接保存。本地模型（Ollama / vLLM）虽然不校验密钥，这里也要填个占位串（如 `ollama`）。

请求走系统配置里的全局代理，与 TMDB 共用一份。

### 分类策略（YAML）
把识别出的媒体按规则归入目录层级，如：

```yaml
电影:
  - genre: "动作,科幻"
    dest: "/电影/动作科幻"
剧集:
  - country: "日本"
    dest: "/剧集/日剧"
```

右侧有实时预览；编辑器带语法高亮。

### 洗版（YAML）
同一部作品出现多个版本时的去留策略。五种模式：

| 模式 | 行为 |
|---|---|
| `coexist` | 共存（都保留） |
| `skip` | 跳过（不动旧文件） |
| `replace` | 替换（新版本覆盖旧版本，旧文件按去向处理） |
| `max_size` | 保留体积最大的版本 |
| `min_size` | 保留体积最小的版本 |

配合条件圈定范围（分辨率、编码、特效等），条件用逗号分隔，`!` 开头表示排除，如 `1080p,hevc,!DV`。

### 重命名
变量模板命名，如 `{title} ({year}) - {resolution}.{ext}`。改的是 **115 网盘内的文件名**（批量执行，防风控）。

### 媒体信息补全（独立 Tab）
对命名不规范的文件（如 `蜘蛛侠.2016.mkv` 缺分辨率/编码）用内置 ffprobe 读取文件头，探测真实画质信息后按策略改名：

- **默认关闭**，需手动开启
- 八种情形各自独立设置「按探测改名 / 保留原名」：信息缺失、命中一致、冲突（高/低保护）、探测失败、命名完整等
- 队列串行处理（每 30 秒一个），不占用同步

## 5. 上传下载

### 监控上传（Emby 元数据回传 115）
Emby 刮削生成的 NFO 与图片保存到媒体目录后，自动回传到 115 对应目录，形成闭环：

1. Emby 媒体库勾选「将媒体封面保存到媒体文件夹」（115-Station 已对目录设置宽松权限 777/666，Emby 可写）
2. 115-Station 监控本地媒体目录，识别标准 Emby 命名（`poster.jpg`、`fanart.jpg`、`seasonXX-xxx.jpg`、`*.nfo` 等）
3. 检测到新文件即上传到 115 同路径目录

无需配置上传目标目录，自动对应全量同步的网盘目录结构。

### 转存下载
- **115 分享链接**：自动转存到「转存目录」（需提取码：填在链接旁的提取码框，或直接附在链接后）
- **磁力 / ed2k / HTTP 链接**：提交到 115 离线任务
- 「转存后整理」开启时，转存/下载完成后自动触发 整理 → 增量同步 全流程
- 支持企业微信机器人远程提交（直接发链接即可，见消息配置）

### 按需离线（边下边播）
提交离线下载成功的那一刻，媒体根的「按需离线/」目录会立即生成占位 STRM，内容指向 115-Station 自己的播放端点（`http://NAS_IP:6086/ed2k/play/<id>`），Emby 扫到即可见可点：

1. 播放时 115-Station 查 115 离线任务状态——没下过就自动提交，下载中返回进度提示（秒传/小文件当场开播）
2. 下载完成后自动定位文件 pickcode，302 跳转 115 直链开播
3. 正式 STRM 由整理 + 同步生成后，占位 STRM 自动移除，不留重复条目

同一链接指纹（ed2k hash / 磁力 btih）全局去重，重复入库不会重复下载。

### RE0（影视转存 · RE0 页签）
RE0（影视资料与分享社区，re0.me）官方 OpenAPI 接入。前提：在 RE0「个人面板 → OPENAPI → 我的应用」创建应用并**等站方审核通过**（公开且站内用户 6+ 的应用，或长期 v 用户），然后：

1. 影视转存 → RE0 页签：填站点地址（默认 `https://re0.me`）、`client_id`、应用 Secret，保存
2. 点「授权 RE0 账号」跳转官方授权页确认一次（Token 自动续期，失效会提示重新授权）
3. 搜索：片名先经 TMDB 匹配，再查 RE0 站内资源（网盘类型/分辨率/大小/解锁积分）
4. 点「解锁并转存」消耗站内积分解锁，115 分享自动转存到接收目录并整理入库；已解锁资源不重复扣积分

RE0 常态屏蔽大陆 IP：115-Station 部署需能直连或为其配置代理。

## 6. 系统配置

> STRM 直链配置已移到 **Strm 管理 → STRM 配置** 页签（见 §3）。

| 配置 | 说明 |
|---|---|
| **TMDB** | API 地址 / Key / 语言 |
| **代理** | 下载附属文件、访问 TMDB 使用的代理 |
| **EMBY 管理** | Emby 地址与 API Key（含测试连接）；**本地路径映射**格式 `本地路径#Emby路径`（两边挂载路径不一致时必须配置，入库刷新/建库插件/6086 反代共用）；路径风格与转换测试；入库时刷新开关。**EMBY入库通知**：自动生成带鉴权 token 的接收地址（可一键复制/换一个），填到 Emby 的 Webhooks 插件（POST + application/json），入库/删除/播放事件即推送到企微/TG |

## 7. 消息配置

### 企业微信（双向机器人）
1. 企业微信后台创建自建应用，获取 **CorpID / Secret / AgentID**；「接收消息服务器配置」的 URL 填 `http://<115-Station公网IP>:6086/wecom/callback`（注意端口是 6086），Token 与 EncodingAESKey 抄到本卡对应输入框
2. **API 地址**默认官方 `https://qyapi.weixin.qq.com`，海外部署访问慢时可改反代地址
3. 在 115-Station 填入凭据并将状态设为「启用」；腾讯要求把服务器 IP 加入应用的「企业可信IP」，否则发送报 60020

对机器人发消息即可远程控制：

| 发送内容 | 作用 |
|---|---|
| 磁力 / ed2k / HTTP 链接 | 提交离线下载（自动整理入库） |
| 115 分享链接 + 提取码 | 自动转存并触发整理（同一条消息发送即可） |
| `状态` | 查看任务与转存目录 |
| `搜索 关键词` | TMDB 搜片 |
| `整理` / `同步` / `补全` | 触发对应任务 |
| `帮助` | 命令列表 |

### Telegram
填 Bot Token 与 Chat ID，用于任务完成通知。

## 8. 扩展功能（插件中心）

卡片式插件列表，可用插件带绿色状态点：

- **🎬 一键创建 Emby 媒体库**（可用）
  - **配置规则**：扫描本地媒体二层目录，预览将建的库（库名 / 类型 / 路径 / 是否已存在）
  - **立即运行**：一键创建全部待建库
  - 自动应用：库名=目录名（如 `/media/电影/国产剧` → 库名「国产剧」）、中文元数据（zh-CN）、NFO/图片保存到媒体文件夹、路径映射
- **🗂️ ALIST 同步**（规划中）
- **🧹 115 文件夹清空**（规划中）

## 9. 实时日志

按任务分类的服务端日志实时滚动（3 秒刷新）。整理 / 同步 / 转存 / 插件 / 机器人操作都有对应前缀（`[整理]` `[同步]` `[插件]` …），可据此排查问题。

## 10. 常见问题

**播放报错 / 黑屏？**
1. 确认 Emby 挂载了同一个 `/media`，路径不一致去 EMBY 配置设置路径映射
2. 播放时看 Emby 播放信息，应为 **DirectPlay**（直连）；出现转码说明文件在转码，检查客户端
3. 手机播放器（空白 UA）失败会自动走服务器中转，无需配置

**提示「服务器开小差了」/ 操作失败？**
115 风控。到账号管理把 API 间隔调大，等几分钟再试；写入操作固定 3 秒间隔无法跳过。

**整理没跑起来？**
检查：整理开关是否开启、待整理目录是否有文件、TMDB 是否配置可用、识别规则是否命中（日志里有每次识别结果）。

**Emby 没有生成 NFO/封面？**
Emby 媒体库需勾选「将媒体封面保存到媒体文件夹」+ 元数据下载器启用；生成后监控上传会自动回传 115。

**忘记密码 / 想改密码？**
修改 compose 里的 `AUTH_PASSWORD` 后 `docker compose up -d` 重启即生效（环境变量为准）。或执行 `docker exec 115-station ./strmhub --reset-admin` 后重启，按环境变量重建（`/data` 内的同步数据保留）。
